org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Loom is not yet configuration-cache compatible.
org.gradle.configuration-cache=false
org.gradle.java.installations.paths=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home

# Minecraft and Fabric API versions are per-target and live in the TARGETS map in
# build.gradle. Check them on https://fabricmc.net/develop
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

mod_version=1.1.0
maven_group=dev.awdllan
archives_base_name=awdl-lan
